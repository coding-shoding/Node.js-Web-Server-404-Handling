# Node.js Web Server Lab

Minimal Node.js server with:
- Homepage (`/`)
- About page (`/about` or `/about.html`)
- Request logging middleware
- Proper `404 Not Found` response for unknown routes

## Run
```bash
npm start
```
Then open http://localhost:3000/

No external npm packages are required.
